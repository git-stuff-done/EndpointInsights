package com.vsp.endpointinsightsapi.authentication;

/**
 * Used to determine which roles on a @PrivateAPI are required, ALL of them or just ONE of them.
 * */
public enum RoleAccessConfigurationType {
	ALL,
	ONE
}
